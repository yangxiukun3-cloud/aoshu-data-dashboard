name: Patch staff realtime chat

on:
  push:
    paths:
      - 'xiaoe-staff/realtime-trigger.txt'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch staff chat
        run: python3 xiaoe-staff/patch_realtime_chat.py
      - name: Commit patched staff page
        shell: bash
        run: |
          set -e
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add xiaoe-staff/index.html
          if git diff --cached --quiet; then
            echo 'No changes'
            exit 0
          fi
          git commit -m 'speed up staff chat with 1s incremental polling'
          git push
